version: 2

sources:
  - name: raw
    schema: gz_raw_data
    tables:
      - name: sales
        identifier: raw_gz_sales_copy
      - name: product
        identifier: raw_gz_product_copy
      - name: ship
        identifier: raw_gz_ship_copy

